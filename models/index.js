const { ContactModel, contactJoiSchemas } = require('./contacts');
const { UserModel, usersValidationSchemas } = require('./users');

module.exports = {
  ContactModel,
  contactJoiSchemas,
  UserModel,
  usersValidationSchemas,
};
